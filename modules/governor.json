{
  "abi": 7,
  "module": "quartz.slice1.governor",
  "version": "0.1.0",
  "execution_mode": "wasm-component",
  "requested_host_capabilities": ["publish-callable"],
  "component": {
    "id": "governor",
    "config_schema": "u64",
    "max_activation_steps": 1,
    "inject": [],
    "provide": [
      {"slot": 2, "kind": "callable", "namespace": "quartz.composition", "interface": "patch-authority", "revision": 1}
    ]
  }
}
